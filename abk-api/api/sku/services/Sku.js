'use strict';
// const shortid = require('shortid');

/**
 * Read the documentation () to implement custom service functions
 */

module.exports = {
    // add: async (values) => {
    //     values.code = shortid.generate();
    //     // Extract values related to relational data.
    //     const relations = _.pick(values, Sku.associations.map(ast => ast.alias));
    //     const data = _.omit(values, Sku.associations.map(ast => ast.alias));

    //     // Create entry with no-relational data.
    //     const entry = await Sku.create(data);

    //     // Create relational data and return the entry.
    //     return Sku.updateRelations({ _id: entry.id, values: relations });
    // }
};
